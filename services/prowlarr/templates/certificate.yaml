apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: prowlarr-tls
  namespace: {{ .Release.Namespace }}
spec:
  secretName: prowlarr-tls
  commonName: prowlarr.homelab.com
  dnsNames:
    - prowlarr.homelab.com
  issuerRef:
    name: homelab-ca
    kind: ClusterIssuer
