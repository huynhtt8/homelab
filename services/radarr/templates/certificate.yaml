apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: radarr-tls
  namespace: {{ .Release.Namespace }}
spec:
  secretName: radarr-tls
  commonName: radarr.home.arpa
  dnsNames:
    - radarr.home.arpa
  issuerRef:
    name: homelab-ca
    kind: ClusterIssuer
