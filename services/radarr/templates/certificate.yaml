apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: radarr-tls
  namespace: {{ .Release.Namespace }}
spec:
  secretName: radarr-tls
  commonName: radarr.homelab.com
  dnsNames:
    - radarr.homelab.com
  issuerRef:
    name: homelab-ca
    kind: ClusterIssuer
