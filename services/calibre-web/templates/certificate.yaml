apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: calibre-web-tls
  namespace: {{ .Release.Namespace }}
spec:
  secretName: calibre-web-tls
  commonName: calibre-web.homelab.com
  dnsNames:
    - calibre-web.homelab.com
  issuerRef:
    name: homelab-ca
    kind: ClusterIssuer
