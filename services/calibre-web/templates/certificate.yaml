apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: calibre-web-tls
  namespace: {{ .Release.Namespace }}
spec:
  secretName: calibre-web-tls
  commonName: library.homelab.com
  dnsNames:
    - library.homelab.com
  issuerRef:
    name: homelab-ca
    kind: ClusterIssuer
