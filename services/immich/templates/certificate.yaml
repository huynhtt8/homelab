apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: immich-tls
  namespace: {{ .Release.Namespace }}
spec:
  secretName: immich-tls
  commonName: immich.homelab.com
  dnsNames:
    - immich.homelab.com
  issuerRef:
    name: homelab-ca
    kind: ClusterIssuer
