apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: jellyfin-tls
  namespace: {{ .Release.Namespace }}
spec:
  secretName: jellyfin-tls
  commonName: jellyfin.homelab.com
  dnsNames:
    - jellyfin.homelab.com
  issuerRef:
    name: homelab-ca
    kind: ClusterIssuer
