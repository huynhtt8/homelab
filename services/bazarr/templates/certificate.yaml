apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: bazarr-tls
  namespace: {{ .Release.Namespace }}
spec:
  secretName: bazarr-tls
  commonName: bazarr.homelab.com
  dnsNames:
    - bazarr.homelab.com
  issuerRef:
    name: homelab-ca
    kind: ClusterIssuer
