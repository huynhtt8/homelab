app-template:
  fullnameOverride: audiobookshelf
  controllers:
    main:
      containers:
        main:
          image:
            repository: ghcr.io/advplyr/audiobookshelf
            tag: latest
          probes:
            liveness:
              enabled: false
            readiness:
              enabled: false
            startup:
              enabled: false
          env:
          - name: TZ
            value: Asia/Ho_Chi_Minh
          ports:
          - name: http
            containerPort: 80
  service:
    main:
      enabled: true
      controller: main
      type: NodePort
      ports:
        http:
          enabled: true
          primary: true
          port: 80
          nodePort: 32378
          protocol: HTTP

  ingress:
    main:
      enabled: true
      className: traefik
      annotations:
        cert-manager.io/cluster-issuer: homelab-ca
      tls:
        - hosts:
            - audiobookshelf.homelab.com
          secretName: audiobookshelf-tls
      hosts:
      - host: audiobookshelf.homelab.com
        paths:
        - path: "/"
          pathType: Prefix
          service:
            name: main
            port: http
  persistence:
    main-library:
      enabled: true
      type: persistentVolumeClaim
      existingClaim: media-share
      advancedMounts:
        main:
          main:
          - path: "/library"
            subPath: "books/audiobookshelf"
    main-config:
      enabled: true
      type: persistentVolumeClaim
      storageClass: longhorn
      size: 1Gi
      accessMode: ReadWriteOnce
      advancedMounts:
        main:
          main:
          - path: "/config"
    main-metadata:
      enabled: true
      type: persistentVolumeClaim
      storageClass: longhorn
      size: 5Gi
      accessMode: ReadWriteOnce
      advancedMounts:
        main:
          main:
          - path: "/metadata"
