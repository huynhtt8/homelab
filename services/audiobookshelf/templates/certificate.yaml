apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: audiobookshelf-tls
  namespace: {{ .Release.Namespace }}
spec:
  secretName: audiobookshelf-tls
  commonName: audiobook.home.arpa
  dnsNames:
    - audiobook.home.arpa
  issuerRef:
    name: homelab-ca
    kind: ClusterIssuer
