apiVersion: v1
kind: Secret
metadata:
  name: copyparty-config
  namespace: {{ .Release.Namespace }}
type: Opaque
data: {}
# Copyparty config is managed out-of-band.
# To inject credentials, run:
#   kubectl create secret generic copyparty-config --from-literal=... -n copyparty
