apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: copyparty-tls
  namespace: {{ .Release.Namespace }}
spec:
  secretName: copyparty-tls
  commonName: copyparty.homelab.com
  dnsNames:
    - copyparty.homelab.com
  issuerRef:
    name: homelab-ca
    kind: ClusterIssuer
