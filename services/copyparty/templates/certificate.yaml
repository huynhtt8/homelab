apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: copyparty-tls
  namespace: {{ .Release.Namespace }}
spec:
  secretName: copyparty-tls
  commonName: share.homelab.com
  dnsNames:
    - share.homelab.com
  issuerRef:
    name: homelab-ca
    kind: ClusterIssuer
